struct IMUData_lcm {
    double quat[4];
    double gyro[3];
    double acc[3];
}

struct JointsState_lcm {
    double qpos[12];
    double qvel[12];
    double tau[12];
}

struct MeasuredState_lcm {
    IMUData_lcm imuData;
    JointsState_lcm jointsState;
    int16_t force[4];
}